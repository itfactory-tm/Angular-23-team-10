name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  back-end:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and Deploy Backend
        run: |
          cd Angular-23-team-10/Back-end
          docker-compose -f docker-compose.yml build
          docker-compose -f docker-compose.yml up -d

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "14"

      - name: Set context to Okteto Cloud
        uses: okteto/context@latest
        with:
          context: ./ # assuming your docker-compose.yml is in the root of Back-end
          token: jFjquV8P0AbTToon1ompj19hpYoU1ZPBOKbT5JHB4Sf7OjKv

      - name: Build and Deploy to Okteto Cloud
        uses: okteto/build@latest
        with:
          file: ./docker-compose.yml
